// src/analyzer/codebase/projectScanner.ts
// ─────────────────────────────────────────────────────────────────────────────
// Phase 2: Walk the workspace and build a list of files to index.
// No VS Code imports — this is a plain Node.js service.
// ─────────────────────────────────────────────────────────────────────────────

// TODO (Phase 2):
// - Walk workspace using Node.js `fs` and respect `.gitignore` / excludePatterns config
// - Return a list of absolute file paths filtered by size limit
// - Report progress via a callback (percent, currentFile)

export {}; // Placeholder export to keep TS happy
